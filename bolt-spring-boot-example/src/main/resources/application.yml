slack:
  bot-token: ${SLACK_BOT_TOKEN}
  signing-secret: ${SLACK_SIGNING_SECRET}

server:
  port: 8080

logging:
  level:
    org.jwcarman.slack.bolt.autoconfigure: DEBUG
    com.slack.api.bolt: DEBUG
